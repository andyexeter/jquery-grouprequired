(function( addTest ) {
	'use strict';

	addTest( 'unit test name here', {
		form: '#selector',
		setup: function( $inputs ) {
			// Do stuff before form is submitted.
		}
	} );

})( $.fn.groupRequired.addUnitTest );
